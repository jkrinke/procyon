# Gradle properties for Procyon project

# Configure JVM options for Gradle daemon
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m

# Enable parallel builds
org.gradle.parallel=true

# Configure daemon
org.gradle.daemon=true
